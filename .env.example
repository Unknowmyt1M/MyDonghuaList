# Copy to `.env` and fill in your Supabase project values when Phase 1 starts.
# Never commit `.env`. Only annotate the key below.
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key